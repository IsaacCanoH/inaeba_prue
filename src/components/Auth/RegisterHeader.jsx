const RegisterHeader = ({ styles }) => (
  <div className={`${styles["header-main"]} bg-primary text-white text-center py-4`}>
    <div className="container">
      <h1 className="fw-bold mb-2">INAEBA</h1>
      <p className="mb-0">Registro de Nuevo Usuario</p>
    </div>
  </div>
)

export default RegisterHeader
